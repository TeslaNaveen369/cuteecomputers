version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install"

  build:
    commands:
      - echo "Preparing static files"
      - mkdir -p output
      - cp -r * output/

artifacts:
  files:
    - '**/*'
  base-directory: output
